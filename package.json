{
  "name": "costa-calendar",
  "version": "2.0.0",
  "private": true,
  "description": "Calendário da Casa da Costa — WhatsApp → JSON → UI. Static GitHub Pages app.",
  "scripts": {
    "build": "./build.sh",
    "deploy": "./deploy.sh",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
